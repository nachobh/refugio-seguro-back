spring:
  application:
    name: backend

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/RefugioSeguro?createDatabaseIfNotExist=true
    username: root
    password: <password_encrypted_later>
